# LessonNotes 專案規則

## 技術棧
- 單檔 HTML,inline CSS/JS,CDN-only,沒有建置步驟
  (跟 ScrollScore / SightScore / HarmonyMap / LoudMaster 同一套部署方式)
- Firebase v10 modular ESM,從 gstatic 的 CDN 動態載入
- 沒有任何相依套件,只有 Google Fonts 與 Firebase 兩個外部來源

## 絕不能做的事
- 不要把單檔拆成多檔案,除非我明確要求
- 不要把 Firebase 改回頂層 `import`。頂層 import 抓不到時整個模組都不會執行,
  連語言與主題切換都會一起死掉。維持動態 `import()` + try/catch,
  載不到只關掉署名入口
- 不要在 `applyTheme()` / `applyLanguage()` 之前 await Firebase。
  順序反過來的話,存了英文偏好的使用者會先看到一整頁中文
- 不要拿掉 `[hidden]{display:none !important}`。`.slip` 是 `display:flex`,
  會蓋掉 `hidden` 屬性,少了這條會留下一張空的錯誤紙條(踩過)
- 圖示一律用線稿 SVG(24 格線、`stroke-width:2`、圓端點、`stroke:currentColor`)。
  唯一的例外是登入鈕上 Google 官方的四色 G,那是品牌規範要求的,
  不要改色也不要換成線稿
- Firebase 的錯誤代碼不要顯示在畫面上。一律經過 `errKeyFor()` 換成一句人話,
  對不到的走 `errGeneric`

## 響應式斷點(與其他四個專案共用,不要自己多開)
```
max-width:600 / 380 / 340     pointer:coarse     prefers-reduced-motion:reduce
```
這一組是五個專案共用的,任何一邊要動都要一起看。需要「隨尺寸連續變化」的東西
一律用 clamp 算,不要為它新增斷點。

## 字型與字級
**版面與配色各走各的,但字型跟其他四個工具一致。** Google Fonts 那一行與
ScrollScore / SightScore / HarmonyMap / LoudMaster 逐字相同。

- `--sans` = `"Noto Sans TC"` 排第一,後面幾個只是 webfont 還沒到位時的暫代。
  **不要改成系統字優先**:系統字在 Windows 會退回 Microsoft JhengHei、
  Android 又各家不同,載 Noto 才能保證每個家長看到的字長得一樣
- `--serif` = `"Noto Serif TC"`,**只用在「封面」**:頂欄的品牌名與署名頁的大標題。
  對話介面(泡泡、清單、輸入框)一律用 sans —— 襯線讀起來是「文件」不是「訊息」
- `--fs-*`:2xl 24 / xl 19 / lg 16 / md 15 / sm 13 / xs 12 / 2xs 11
- 輸入框字級不得小於 16px(見下面「輸入框與縮放」)

## 配色
**淺色:暖灰 + 白 + 霧面藍綠(莫蘭迪 × 蒂芬妮)。深色:工具箱家族的黑金。**

- 淺色 `--bg:#F1EEE9`(暖灰,不是藍調的冷灰)/ `--panel:#FFFFFF` / `--gold:#45677F`
  (莫蘭迪的霧藍:低彩度、帶灰,配暖灰底正是他常用的那組對比)。
  **整頁的「暖」有一半是中性色決定的,不是重點色決定的** —— 底、文字、灰階全部
  帶一點米,冷灰換暖灰的效果比換重點色還明顯
- **真正的蒂芬妮藍 `#0ABAB5` 不能直接用**:太亮,白字壓上去只有 2.2:1,
  當按鈕或泡泡底色都讀不動。要用那個色相就得壓深、降彩度
- **署名鈕自己一個顏色** `--cta:#1669D6`,比重點色更亮更藍。署名頁上幾乎沒有
  別的東西,那顆鈕要跳出來;霧面的重點色在那裡會太退。
  簿本裡的「寫進簿本」「存起來」維持 `--gold` —— 那些是在表單裡按的,不必搶
- 自己的泡泡用**淡色底 + 深色字**(`--out-bg:#D7E2EB` / `--out-ink:#2E2B28`),
  不是實心重點色。一整串飽和色塊會吵,淡色也比較符合「柔和」。
  三層表面的明度是 **白(對方) > 底 > 淡霧藍(自己)**,各自分得開
- 深色 `--bg:#0C0A07` / `--gold:#C9A24B` —— 與 ScrollScore / HarmonyMap /
  LoudMaster **同一組值,這兩個才是黑金的身分,不要動**
- **但深色的「面」比家族原值亮一階:`--panel:#2A2217`(家族是 `#161209`)。**
  `#161209` 壓在 `#0C0A07` 上只有 **1.06:1** —— 那等於沒有分層,名冊那一列與
  對方的泡泡會整個融進背景裡(踩過)。那三個工具是「一整片面鋪滿一頁」,
  分不分得開沒差;對話介面靠三層表面吃飯。
  `#2A2217` 對底是 1.26:1(LINE 深色 1.16、WhatsApp 1.30),對自己的泡泡
  `#4A3A15` 還留著 1.42:1。**理由與下面「SightScore 的淺色沒有直接搬過來」
  完全同一條:色相跟家族同一套,明度階不能照抄**
- 面一亮,`--panel-2`(hover 與頭像底)與 `--line`(列與列之間那條線)要一起亮,
  不然 hover 會變暗、分隔線會消失。深色的 `theme-color` meta 也要跟著改成新的面
- 深色三層表面是**遞增**的:底 `#0C0A07` < 面 `#2A2217` < 自己的泡泡 `#4A3A15`
  (淺色剛好相反:白的對方 > 底 > 淡霧藍的自己)。兩邊都是三層分得開,
  方向不同不是問題 —— WhatsApp 深色的自己泡泡也比對方亮
- 自己的泡泡在深色下用**暗金** `--out-bg:#4A3A15`,不要用亮金 `#C9A24B`。
  亮金配近黑字很漂亮,但一整串訊息全是亮金會很吵 ——
  WhatsApp 深色模式的自己泡泡也是暗綠不是亮綠
- **深色跟著系統走(`prefers-color-scheme`),沒有切換鈕、沒有 `sm_theme`、沒有 JS**。
  那四個通訊軟體的 App 裡都沒有主題開關。`theme-color` 那兩個 meta 各自掛 `media`,
  也不需要 JS
- 變數名稱沿用舊的語意名,所以元件的程式碼不用動:`--gold` = 重點色、
  `--ivory` = 主要文字、`--panel` = 面。**值換掉就好,不要重新命名**
- 泡泡裡的文字用 `color:inherit`,不要寫死 `--ivory` —— 深色下自己的泡泡是
  `--out-ink`,寫死會看不見
- **課堂紀錄的記號不另外給一個顏色**,用 `--gold-edge` 就好 ——
  一般訊息本來就沒有那條線,有線就是紀錄,不需要第二種色相去說同一件事

### SightScore 的淺色配色為什麼沒有直接搬過來
它的 `--bg:#FBF7EE` 與 `--panel:#F4EEE0` **只差約 7% 亮度**,而且它的註解寫明
「刻意沒有任何陰影、沒有大圓角卡片」。對話介面靠三層表面吃飯(底 / 對方的泡泡 /
自己的泡泡),底跟面差 7% 的話對方的泡泡會融進背景裡,泡泡就不成立了。
**色相可以跟家族同一套(深色就是),明度階不能照抄。**

## 文案
寫成紙本聯絡簿的口氣:**署名**、**翻開**、**闔上簿本**、**簿本編號**。
不要出現「連線成功」「使用者編號」「認證失敗」「登入」這類系統用語 ——
這一頁是給家長看的,不是給工程師看的。

## 中英雙語
- 只維護一份 DOM,靠 `data-i18n` / `data-i18n-attr` / `data-i18n-html` +
  `applyLanguage()` 換字
- 動態寫進畫面的字(扉頁欄位、錯誤紙條、按鈕的忙碌狀態)不吃 `data-i18n`,
  `applyLanguage()` 結尾要重畫一次
- **英文介面不附中文對照**。新增文案時 `zh` 與 `en` 兩邊都是必填
- `sm_lang` 與其他四個工具共用(主題偏好 `sm_theme` 這一頁不用)

## 資料與角色
- 老師由 `index.html` 的 `TEACHER_EMAILS` 認定。**`firestore.rules` 裡有一份
  一模一樣的名單,兩邊一定要一起改** —— 前端那份只決定畫面長怎樣,真正擋人的是規則。
  只改前端等於沒改
- 家長只讀寫得到 `parentUid == 自己` 的簿子。不要為了方便加一條「登入就能讀全部」
  的規則,那等於所有家長互相看得到對方的孩子
- `parentUid` 不可以被 update 改掉,不然簿子會被搬走(規則裡有擋)
- 只有老師寫得出 `kind == 'lesson'`。家長一律是 `reply`,規則跟前端各擋一次。
  老師也寫得出 `reply`(要回家長一句話又不想留下上課日期時),所以判斷「這是誰寫的」
  要看 `authorRole`,不要用 `kind` 反推
- **家長不需要等老師先寫**。輸入框永遠在,簿本全空時也送得出去 ——
  家長請假、問問題本來就常常發生在課堂紀錄之前。空狀態的文案要講清楚可以先寫,
  不能只寫「老師還沒寫下第一堂課」,那看起來像還不能動
- `lastEntryBy` 記最後一筆是誰寫的,名冊靠它標出「家長寫了但還沒回」的簿本。
  少了這個標記,家長寫了訊息而老師沒翻開那一本,訊息等於石沉大海
- **在等回覆的簿本一律排到名冊最前面**,標題底下也寫出有幾本在等。只標顏色不排序的話,
  20 本裡的 3 本會散在清單各處,還是會漏。其餘照名字排(交給瀏覽器的 zh-Hant 定序,
  繁中是筆畫序),位置才穩定
- **一本簿子 = 一個孩子,一堂課只是裡面的一筆 entry**。不要做成「一堂課一本」,
  那會讓名冊爆量、也看不出同一個孩子的進度
- 寫完之後畫面交給 `onSnapshot` 更新,**不要自己再塞一筆進時間軸**,會看到兩筆
- `serverTimestamp()` 在伺服器回話之前是 null,本地那一筆會先以 null 出現。
  顯示時間一律走 `stamp()`,它會在那一瞬間顯示「剛剛」而不是 1970 年

## 署名怎麼顯示
- **身分收在頂欄的頭像後面**(`#meBtn` → `#meMenu`),不是常駐在頁尾的一條 who-bar。
  那四個通訊軟體都是這樣:正常使用的人一輩子按不到一次登出,不該每一頁都佔著位置。
  選單裡才有名字、老師徽章與「闔上簿本」
- **但「闔上簿本」不能整個拿掉。**`prompt:"select_account"` 每次都讓人自己挑帳號,
  挑錯是會發生的(很多人有私人和工作兩個 Google 帳號)。家長用錯帳號進來會停在
  「取名字」那一頁,沒有出口的話他只能照著打名字 —— 結果是在錯的帳號底下開出一本
  空簿子,老師的名冊上多一個對不到人的孩子;不然就是卡住,除非會清網站資料。
  **頭像那行字負責診斷「你走錯了」,這顆鈕負責治療,只留診斷等於把門鎖上**
- 選單要能用**點外面**和 **Esc** 收起來,`show()` 換頁時也收一次 ——
  它是 `position:absolute`,留在畫面上會蓋住簿本
- 老師的頭像鑲一圈 `--gold-edge`。徽章收進選單之後,頂欄仍然看得出現在站在哪一邊
- 沒有 `photoURL` 的時候換成線稿的人(`#whoFallback`),不要留一個空圓圈 ——
  空圓圈看起來像圖還沒載完
- 選單裡那行字對家長顯示**孩子的名字**(多個孩子就列出來),不是 Google 顯示名稱,
  也不加「的家長」—— 頭像加名字已經說明是誰,簿本抬頭又在同一頁上。
  帳號認錯了還是看得出來:名字是從「這個帳號自己的簿子」推出來的,
  登錯帳號會看到別的孩子或回到取名字那一頁
- 家長那一筆顯示「**{孩子的名字}的家長**」,不要顯示 Google 帳號的顯示名稱。
  老師的名冊上二十個家長各自叫什麼英文名字,對不上是哪個學生
- 這個名字**每次重畫時從 `current.name` 現算**,不要寫進 entry 的資料裡。
  寫死的話改了孩子的名字之後,以前的紀錄還掛著舊名字
- 老師那一筆照舊顯示 `authorName`(老師只有一位,名字有意義)

## 輸入框與縮放
- **所有 input / textarea 的字級不得小於 16px**。iOS Safari 只要輸入框字級 < 16px,
  點進去就會自動把整頁放大、版面跟著跳 —— 那才是「畫面亂跳」的真正原因。
  用 `--fs-lg`(16px),不要用 `--fs-md`(14px)(踩過)
- **不要加 `user-scalable=no` / `maximum-scale=1`**。家長要放大看樂譜照片、
  年長的家人要放大看字;而且 iOS 從 10 開始根本不理這個設定,加了只是在
  Android 上傷到需要的人。雙擊放大已經被 `touch-action:manipulation` 關掉了,
  那個才是會誤觸的手勢,雙指縮放要留著

## 作業清單
- 老師在課堂紀錄的「這週要練的」一行寫一項,`parseTasks()` 切成 `tasks: [{t, d}]`
- **打勾的是家長,但作業掛在老師那一筆上**,所以家長要改得動別人寫的文件。
  規則用 `diff(resource.data).affectedKeys().hasOnly(['tasks'])` 把範圍鎖死在
  這一個欄位 —— 其他欄位一起被改的話那條規則就不成立,課文改不掉、作者也換不了
- 老師改作業文字時,**文字沒變的項目要保留原本的勾**。整串重設的話老師調一個錯字,
  家長整週的進度就歸零了(`editBox` 裡用 `old.find(o => o.t === x.t)` 比對)
- `lastTasks: {d, n}` 寫在 student 上,名冊才不用點進去就看得到進度。
  **出作業的當下就要寫**(`addEntry` 裡),不要等第一個勾才寫 —— 老師剛出完作業
  回到名冊會看到一片空白,那正是他最想看到「0/3」的時候(踩過)

## 改寫與刪除
- **誰都碰不到別人寫的東西,老師也一樣。** 家長的請假、回報、申訴是他自己的紀錄,
  不該被另一方修改或抹掉。`canEdit` / `canDelete` 都要求 `authorUid === me.uid`
- 因此**簿本不能整本刪**(刪父文件會留下只有作者刪得掉的孤兒子集合)。
  「不要了」的簿本走**封存**(`students.archived`):從名冊收起來,資料原封不動,
  隨時取得回來。老師與該簿本的家長都按得動。**封存的入口在名冊上左滑**,不在簿本內頁
- **家長:改寫與刪除都限 15 分鐘(`PARENT_WINDOW_MIN`)。老師:不受限**。
  需要事後改正的是老師(小節數寫錯、譜名打錯,家長已經照著練了還是得改對);
  家長那邊寫出去的是說過的話,老師可能已經照著排課了
- **打勾不受時限**。作業是好幾天後才練完的,`hasOnly(['tasks'])` 那條規則獨立於
  改寫那條,不要把時間條件加進去(加了等於作業做完也打不了勾)
- 改過的一定要標「已修改」。悄悄改掉等於讓對方對不上他看過的版本
- 時限在 `firestore.rules` 裡也擋一次(`request.time < createdAt + duration.value(15,'m')`),
  前端不給按只是裝飾
- **`editingId` 要在 `await` 之前就清掉**。Firestore 有本地的樂觀更新,`updateDoc`
  還沒回來 `onSnapshot` 就先重畫一次了;那時候 `editingId` 還在的話表單會留在畫面上,
  看起來像沒存進去(踩過)。存失敗再把它設回去
- **叫出「複製 / 改 / 刪」的手勢是長按泡泡**(`wireLongPress`),不是點一顆 ⋯ ——
  每一個通訊軟體都是這個手勢,比每一筆都掛一顆小小的 ⋯ 好按得多,時間軸也乾淨。
  桌機沒有長按,所以:`.more-btn` 只在 `(hover:hover)` 的裝置上存在、滑過那一列
  才浮出來(Tab 到它也會出現),**右鍵**是同一個入口。`pointer:coarse` 上
  `.more-btn` 是 `display:none`
- 長按的判斷:480ms;**手指移動超過 8px 就取消**(那是在捲簿本);滑鼠不接長按
- **長按放開之後瀏覽器會補一個 `click`**,要吞掉那一次(`actsJustOpened`),
  不然選單一開就被自己關掉。但**右鍵那條路不可以設這個旗標** ——
  右鍵後面不會補 click,設了會把使用者接下來真的想關掉的那一下吞掉(踩過)。
  用計時器當唯一判斷也有同樣的毛病,要「吞掉下一次」而不是「吞掉一段時間」
- 選單要能用**點別的地方**和 **Esc** 收起來
- **「複製」誰都有**(包括動不了那一筆的人)。手機上泡泡的文字選不起來
  (`user-select:none`,長按被我們接走了),少了這一顆就沒辦法把譜名、小節數
  抄去別的地方。改與刪仍然只有作者本人有
- 選單打開時那一筆要圈起來(`.bubble.is-acted`),不然選單長在下面,
  看不出是哪一筆的
- 刪除一律先問一次,問句就長在那一筆上(不用 `confirm()`)。封存也不問 —— 見下面那一節
- **刪紀錄要先收附件再刪本體**。順序反了的話 media 文件會變成沒人指得到的孤兒
- Firestore **刪父文件不會連子集合一起刪**。刪整本簿子要自己把 entries 與 media
  一筆一筆收掉

## 時間軸的樣子(照通訊軟體的做法)
- **自己寫的靠右(`.row--out`)、對方寫的靠左(`.row--in`)**。「自己」是**看的人自己**,
  所以判斷用 `en.authorUid === me.uid`,**不要用 authorRole** —— 同一筆課堂紀錄,
  老師看在右邊、家長看在左邊
- **頭像與名字只在對方那一側**。自己是誰不必告訴自己,重複顯示只是雜訊
- 老師的大頭貼寫進 entry 的 `authorPhoto`,家長那邊才顯示得出來(家長讀不到
  老師的帳號)。家長的大頭貼**不存** —— 那張臉跟孩子對不起來,反而更難認
- 時間、「已修改」、⋯ 都放在**泡泡外面**的 `.meta` 那一列,不佔泡泡裡的空間
- **日期分隔線**(今天 / 昨天 / 日期)。簿本一長,沒有分隔線就只剩一堆泡泡
- 時間顯示:今天只給時分、昨天寫「昨天 HH:MM」、更早給日期(`fmtWhen`)。
  分隔線與時間都用**寫下來的時間**;課堂紀錄自己的上課日期另外標成一顆晶片
  (`.chip` + `lessonOn`),兩者不要混在一起
- 課堂紀錄的泡泡加 `.is-lesson`(頂上一道金線)。它是簿子的主體,不只是一句話

## 對話清單(名冊)
- 一列一本簿子:**頭像 + 名字 + 最後一句話 + 時間**,跟所有通訊軟體一樣。
  整份清單是**一張白色的面**,列與列之間只有一條細線 —— 不是一堆各自獨立的卡片
- 預覽那一行的優先順序:封存 → 最後一句(`lastText`)→ 作業進度 → 上課時間
- `lastText` 在 `addEntry` 裡寫進 student(前 60 字)。少了它,清單上只剩名字跟時間,
  看不出那一本發生什麼事
- 「在等你回」用**一顆點 + 預覽文字變色**,不要用文字說明 —— 那是未讀標記的語言

## 輸入框永遠釘在最下面(簿本頁是聊天畫面,不是文件)
- **簿本那一頁整頁不捲,只有 `#nbScroll` 自己捲**(`body.nb-mode`)。
  `show()` 進 `notebook` 時掛上這個 class,離開時拿掉 —— 名冊與署名頁還是
  一般的文件,照常捲、照常有頁尾
- **不要退回 `position:sticky`**。sticky 只在「父容器的範圍」裡有效,底下還有
  `main` 的內距與頁尾,捲到底時輸入框就被推上去了(踩過)。
  真正的聊天畫面不是把輸入框釘住,是**整頁根本不捲**
- `body.nb-mode` 的高度吃 `--vvh`(`visualViewport.height`),沒有的話退回 `100dvh`。
  手機鍵盤彈出來時 dvh 不會縮,少了這一條輸入框會被鍵盤蓋住
- `nearBottom()` / `toBottom()` 量的是 `#nbScroll`,**不是 `window`**
- **打開簿本、或自己剛寫完,要停在最新的一筆**(`stickBottom`)。其餘情況只有在
  使用者本來就在底部附近(`nearBottom()`)時才跟著捲 —— 他正在往上翻舊紀錄時
  硬把他拉回來最惱人
- 內頁橫線只留給「整張都是文字」的卡片。有輸入框的那幾張加 `.sheet--plain`,
  橫線壓在輸入框與按鈕上看起來像沒對齊

## 底下那一列就是輸入框(不是「點了才展開」的殼)
- **`#replyInput` 永遠在,永遠是一個真的 textarea。**通訊軟體的輸入框沒有收合態 ——
  少一次點擊,也不必猜那條東西是什麼。**不要再做回「點了才展開」**
- 那一列是 `[＋] [輸入框] [寫下這堂課 或 送出]`。
  **「寫下這堂課」與送出鈕輪流出現**:空的時候是前者,有字或有附件才換成送出。
  兩顆同時掛著的話 320px 上就沒剩多少地方打字(WhatsApp 的麥克風/送出也是這樣換)
- `＋`(`#attToggle`)才打開三顆附件鈕。課堂紀錄那張表單則是一直顯示它們
- **`autoGrow()` 空的時候一定要直接 return,不要量。**空的 textarea 的
  `scrollHeight` 會把「會換行的提示語」算進去 —— 提示語一長,那一條空著就從
  一行變成兩行高(踩過)。空的交給 CSS 的 `min-height`
- 連帶地:**那一條的提示語要短**(一行放得下)。引導的話留給空狀態那一段文案,
  聊天框不是表單欄位
- 桌機 Enter 送出、Shift+Enter 換行;`pointer:coarse` 一律換行 ——
  虛擬鍵盤上沒有 Shift 可以按
- **送出要有 `sending` 旗標擋連按。**那一條上的送出鈕與「寫進簿本」是同一個
  處理函式,只把(藏起來的)`#sendBtn` 設成 disabled 擋不住 `#replySend`,
  連按兩下會送出兩筆(踩過)

## 課堂紀錄才是一張表單
- **點本子鈕 `#lessonBtn` 才開課堂紀錄**(`composerMode = "lesson"`),
  打字一律是回一句(`reply`)。不要再做「課堂紀錄 / 回一句」的切換鈕 ——
  多一層選擇等於每回一句話都要先想「我現在是哪一種」
- 老師回訊息是隨時的、寫課堂紀錄一週一次,所以**常用的那個是零步驟**
- 收起之後回到那一條輸入框(`composerMode` 重設為 `reply`)
- 送出鈕的字:課堂紀錄是「寫進簿本」。那一條上的送出鈕只有圖示

## 展開後的版面(拇指在畫面下緣)
- **「收起」跟「寫進簿本」在同一列,在表單最下面**(`.composer-acts`),
  不要放回抬頭右上角 —— 手機上那一角要整隻手換位置才按得到。
  收起在左、送出在右。Esc 也要收得起來(托盤開著就先收托盤)
- **表單的捲動上限用 flex 算,不要寫死 dvh**:`#composer.is-lesson{max-height:82dvh}`
  加上 `.composer-fields{flex:1 1 auto;min-height:0;overflow-y:auto}`。
  寫死 `58dvh` 那種做法在瀏覽器工具列比較高的手機上會剛好切掉最底下那一段(踩過)
- **三顆附件鈕、錄製中那一列、附件預覽、錯誤紙條一律在捲動區外面。**
  放進去的話表單一長就被切掉,看起來像功能消失了;正在錄音的時候
  「停下來」被捲走更是最糟的情況
- 上課日期收成**一列**(`.field-row.inline`,標籤在左、日期在右),省下一整行
- `#bodyInput` 起跳 168px —— 課堂紀錄一次要寫好幾行。改寫用的框也要一樣會長
- **三顆附件鈕只放圖示**(`.att-btn`),名稱走 `aria-label` / `title`。
  三段文字在手機上會換行成兩列,佔掉的版面比它們說明的事情還多

## 這本簿子的設定收在 ⋯ 後面
- 抬頭右邊是一顆 ⋯(`#nbMoreBtn`),裡面是「改名字」。
  **不要把「改名字」掛回抬頭** —— 那是一年按一次的動作卻佔著最好的位置,
  而且就長在孩子的名字旁邊,很容易誤按
- 開關的規則跟頂欄那個選單一樣:點外面、按 Esc、`show()` 換頁都要收起來

## 封存在名冊上左滑(不在簿本內頁)
- **要收起來的那一本,是在名冊上看到它的時候才想收的**,不必先翻開。
  左滑那一列,右邊露出「封存」(`.rrow-act`)—— 通訊軟體的清單都是這個手勢
- 一列的結構是 `.rrow`(裁切 + 藏著那顆鈕)> `.roster-item`(會位移的那一層)
  + `.rrow-act`(絕對定位貼右緣)
- **`.roster-item` 一定要 `z-index:1`。** 兩個都是 positioned、`z-index` 都是 auto
  的話,DOM 在後面的封存鈕會畫在上面 —— 那顆鈕就不是「藏在後面」而是「蓋在右邊」,
  點那一列的右端會按到封存,手指也滑不動(踩過)
- **鍵盤那條路用 CSS 的 `:has(.rrow-act:focus-visible)`,不要用 JS 聽 `focus`。**
  重畫名冊之後瀏覽器會自己搬動焦點,用 JS 聽的話下一列會自己彈開,
  接著那一下點擊就落在錯的簿子上,一次封存兩本(踩過)
- `setArchived` 要有 `archiving` 旗標擋連按。寫入還沒回來就再按一次,
  會封存到重畫後站在那個位置的另一本
- 手勢判斷:先比 `dx` 與 `dy`,**直向的一律放掉**(那是在捲名冊),
  `.roster-item` 也要 `touch-action:pan-y`
- **滑開的時候點那一列只收回去,不翻開簿本**;點外面、Esc、捲動、換頁也都收回去
- **不用 `confirm()`**。左滑再按是兩個刻意的動作,而且「顯示已封存的」開關就在
  同一頁上,取回只要一下。在滑動手勢上彈一個系統對話框是通訊軟體不會做的事
- 已封存的那一列滑開後寫的是「取回」,不是「封存」

## 多個孩子
- 家長只有一個孩子時直接翻開那一本,不要逼他每次多點一次清單;
  **不只一個才顯示清單**,標題換成「我的孩子」,並出現「加一個孩子」
- 「回到名冊」的顯示條件是 `amTeacher || students.length > 1`

## 附件(錄音、照片、影片)
- **兩條路,依大小分流,不要統一成一條**:錄音與照片壓完只有幾百 KB,以 base64 存進
  `students/{id}/media/{entryId}`(Firestore),不用物件儲存、不用綁卡;影片放不進
  1 MiB 的文件上限,只能走 Firebase Storage(需要 Blaze)
- 位元組**一定要跟 entries 分開放**。時間軸一次讀 50 筆,位元組混在裡面的話光是
  捲動簿本就會拉下幾十 MB。時間軸只放一顆按鈕,按了才讀那一筆
- `INLINE_MAX` 是 650000 —— base64 之後約 867 KB,離 1 MiB 還有餘裕給其他欄位。
  規則那邊也擋 `data.size() <= 900000`,兩邊要一起改
- 照片一定要在瀏覽器裡先縮(`shrinkPhoto`,長邊 1280 / JPEG 0.75)。手機直出 4 MB,
  不縮的話一張就爆掉
- 錄音上限 90 秒、錄影 60 秒,到點自動停 —— 不要靠使用者盯著計時器
- `navigator.mediaDevices` 只在**安全來源**才存在。整個不在的時候是 `errNoRec`
  (這個瀏覽器不能錄音),不是 `errMic`(請允許麥克風)—— 叫人去允許一個不存在的
  權限只會讓他找不到那個設定。線上是 GitHub Pages(HTTPS)沒問題,本機測試要用 HTTPS
- 附件失敗時**不要把已經寫好的那筆 entry 刪掉**。先寫 entry 拿到 id,附件再掛上去;
  附件掛失敗至少文字還在
- 只錄一段不打字也要送得出去。規則要求 `body` 不能是空字串,所以純附件時前端拿
  附件種類當 body

## 設定與部署
- `firebaseConfig` 六個欄位只要有一個是 `PASTE_HERE`,整頁換成「簿本還沒裝訂完成」,
  連 Firebase 都不初始化
- `apiKey` 不是密鑰,部署出去的原始碼裡本來就看得到。擋門的是 Authentication 的
  已授權網域清單(`stevenmusic.github.io` —— 只填主機名稱,不要協定也不要路徑)
- GitHub Pages 從 `main` 的根目錄部署。改完推上 main 之後要確認
  Actions 有跑出一筆 `pages build and deployment`;沒跑出來的話線上還是舊版,
  跟瀏覽器快取無關(踩過)

## 改完要確認的事
1. 兩種語言 × 淺色/深色(用系統偏好模擬)各看一遍,英文版畫面上不能有中文
   (語言鈕的「中」除外)
1b. 深色下名冊那一列與對方的泡泡都要明顯浮在背景上。測試有算對比度
   (面對底 ≥1.2、面對自己的泡泡 ≥1.2、線對面 ≥1.15),不要只靠眼睛
2. 320px 寬不能出現橫向捲軸
3. 這幾種狀態都要看:未設定、未署名、CDN 連不上、資料庫沒開(規則擋住)、
   家長第一次(取名字)、家長已有簿子、老師的名冊、老師翻開某一本
4. 家長與老師各跑一遍:家長不能有「上課日期」欄位、「二選一」與「回到名冊」,老師要有
5. 老師直接打字寫出來的是 reply(沒有上課日期欄、沒有作業欄);
   按了本子鈕才是 lesson。收起再打開要回到 reply。
   輸入框空著時是「寫下這堂課」、有字時是送出鈕,兩顆不會同時出現
6. 名冊至少放三本、其中一本由家長寫最後一筆,確認它排在最前面且統計行算對
7. 錄音、錄影、照片各送一次,確認時間軸的按鈕按得開;沒開 Storage 時影片要給人話
8. 改一次孩子的名字,確認舊紀錄的署名跟著改口
9. 改寫一筆、刪一筆、刪整本各做一次;老師改作業文字後家長的勾要還在
10. 把一筆的 createdAt 往前推,家長那邊連 ⋯ 都要消失,但作業還是打得了勾;
    老師自己的舊紀錄仍動得了,而且老師對家長那一筆完全沒有 ⋯
11. 在名冊上左滑封存一本,再從開關叫回來(已封存的那一列滑開寫的是「取回」),
    確認資料都在;直向拖曳不能滑開任何一列;滑開時點那一列只收回去
11b. 簿本捲到最上面與最下面各一次,輸入框都不能移動;展開課堂紀錄、再把內文
    打到十幾行,送出鈕仍要在畫面內。那一條打到八行再清空,高度要縮回一行
12. 老師與家長各看同一本簿子一次,確認左右剛好相反,而且名字只出現在對方那側
12b. 長按泡泡叫得出「複製 / 改 / 刪」;在泡泡上往上滑不能叫出來(那是在捲簿本);
    點別的地方與 Esc 都收得起來;動不了那一筆的人只看得到「複製」
13. 那一條輸入框永遠在(`#replyInput`),不用先點開;課堂紀錄要先點 `#lessonBtn`,
    回一句的三顆附件鈕要先點 `#attToggle`;
    「改名字」與「封存」要先點開 `#nbMoreBtn`;
    同理「闔上簿本」收在選單裡,要先點開 `#meBtn`
14. CDN 連不上之後切一次語言,署名鈕必須還是停用的(`unavailable` 旗標,踩過)
